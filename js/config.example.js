/**
 * config.example.js — Appwrite project identifiers
 *
 * Copy this file to config.js and fill in your values:
 *   cp js/config.example.js js/config.js
 *
 * config.js is listed in .gitignore so real IDs are never committed.
 * These IDs are used by appwrite-sync.js to reach your Appwrite project.
 *
 * Find them in the Appwrite console:
 *   PROJECT_ID   → Project Settings > Project ID
 *   DATABASE_ID  → Databases > your database > Settings > Database ID
 */

export const APPWRITE_ENDPOINT = 'https://sfo.cloud.appwrite.io/v1'
export const APPWRITE_PROJECT_ID = 'YOUR_PROJECT_ID_HERE'
export const APPWRITE_DATABASE_ID = 'YOUR_DATABASE_ID_HERE'
