# Real Appwrite credentials — use js/config.example.js as a template

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/settings.json

# Node (if a build step is added later)
node_modules/
dist/
.env
.env.local
